using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreController
{
    /*
    private int _score;
    private LevelController _levelController;


    public int Score { get => _score; set => _score = value; }

    public ScoreController(ObjectView player, LevelController levelController)
    {
        _player = player;
        _levelController = levelController;
        _player.OnContact += OnScoreUp;
    }

    private void OnScoreUp(ObjectView contactObject)
    {
        if (_levelController.Levels[_levelController.CurrentLevelIndex]._obstacles.Contains(contactObject))
        {

        }
    }
*/
}
